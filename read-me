Search for a word in a file or directory recursively using argparse in python. 74 lines so far.

I tried to make it output as near as the bash version below which is just one line.

	grep -ri searchword /home/andy/allmypython 

------------------------------------------------------------
PYTHON:

USAGE:

	python2 regargsFile.py <WORD> <[-f|-d]> <[FILENAME|DIRECTORY]>

RUN EXAMPLES:

	python2 regargsFile.py import -d allMyPython
	python2 regargsFile.py argparse -f allMyPython/filename
	python2 regargsFile.py blahblah -f allMyPython/serv/filename
